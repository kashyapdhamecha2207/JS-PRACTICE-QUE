20. Find and print the difference between the maximum and minimum elements in arr = [80, 30, 70, 50, 20].

var arr=[80,30,70,50,20];
var max=arr[0];
var min=arr[0];

for(i=0;i<arr.length;i++){
  if(max<arr[i]){
    max=arr[i];
  }
  if(min>arr[i]){
    min=arr[i];
  }
}

var dif=max-min;
console.log(dif);


21. Write a program to convert a given string to uppercase.Example: Input: "hello", Output: "HELLO".

function xyz(str){
  return str.toUpperCase();
}

let input = "hello";
let output = xyz(input);
console.log(output);

22. Write a program to find the length of a given string.


let str="Hello, World!"
var len=str.length
console.log(len)

23. Write a program to concatenate two given strings. Example: Input: ("hello", " world"), Output: "hello world".


let text1="hello"
let text2="world!"
let text3=text1.concat(" ",text2)
console.log(text3)


24. Write a program to remove whitespace from both ends of a string.Example: Input: " hello ", Output: "hello".


let text1=" hello world! "
let text2=text1.trimStart();
console.log(text2);

25. Write a program to split a string into an array of words.


let text1= "Hello world, welcome to JavaScript!"
var arr=[text1.split(",")]
for(var i=0;i<arr.length;i++){
    console.log(arr[i])
}

26. Write a program to check if a string ends with a specific character.Example: Input: ("codinggita", "a"), Output: true.


function endsWithChar(str, char) {
    return str.endsWith(char);
}

let inputStr = "codinggita";
let charToCheck = "a";
let output = endsWithChar(inputStr, charToCheck);
console.log(output); // Output: true

27. Write a program to extract the file extension from a given filename.Example: Input: "document.pdf", Output: "pdf".


function getFileExtension(filename) {
    
    let parts = filename.split('.');
    
    
    if (parts.length === 1 || (parts[0] === '' && parts.length === 2)) {
        return '';
    }
    
    
    return parts.pop();
}
let filename = "document.pdf";
let extension = getFileExtension(filename);
console.log(extension);  
    
28. Write a function that takes two numbers and prints the largest one.


function largestnumber(n1,n2){
    if(n1<=n2){
        console.log(n2)
    }else{
        console.log(n1)
    }
}
let n1=10;
let n2=20;
let result=largestnumber(n1,n2)
console.log(result)

29. Write a program to find all pairs in an array whose sum is equal to a given number.
Example 1:  Input: nums = [2,7,11,15], target = 9 , Output: [0,1].


let nums = [2, 7, 11, 15];
let target = 9;
for (var i = 0; i < nums.length; i++) {
    for (var j = i + 1; j < nums.length; j++) {
        if (nums[i] + nums[j] == target) {
            console.log("Indices:", i, j);
        }
    }
}

30. Given an integer array nums, find the contiguous subarray (containing at least one number) which has the largest sum and return its sum.Example1. Input:nums = [-2, 1, -3, 4, -1, 2, 1, -5, 4], Output: 6  //The subarray [4, -1, 2, 1] has the largest sum = 6.


let n = "123456";
let len = n.length;
let evensum = 0;
let oddsum = 0;

for (var i = 0; i < len; i++) {
    let digit = Number(n[i]);  
    if (digit % 2 == 0) {  
        evensum += digit;
    } else { 
        oddsum += digit;
    }
}

console.log("Even Sum:", evensum);
console.log("Odd Sum:", oddsum);

31. Given an array of characters, compress it in-place. The length after compression must be the same or smaller than the original array. Compression should be done using the following rules:
Count consecutive repeating characters and store the character followed by the count.Example:  Input: ["a","a","b","b","c","c","c"],Output:["a","2","b","2","c","3"].


function repeatstr(str, n) {
    let result = "";
    for (let i = 0; i < n; i++) {
        result += str;  
    }
    return result;
}

let str = "hello";
let n = 3;
let result = repeatstr(str, n);
console.log(result);  

32. Given an array nums of n integers where n > 1, return an array output such that output[i] is equal to the product of all the elements of nums except nums[i].Example:Input: [1, 2, 3, 4] Output: [24, 12, 8, 6].


let age=14;
if(age<=0){
    console.log("not valid age")
}else if(age>0 && age<13){
    console.log("Child")
}else if(age>=13 && age<=19){
    console.log("teenager")
}else if(age>=20 && age<=59){
    console.log("adult")
}else if(age>=60 && age<=100){
    console.log("senior")
}

33.To sort an array containing only 0s, 1s, and 2s, you can use the Dutch National Flag algorithm, which efficiently sorts the array in a single pass. Here's how you can implement it: Example: Input: [2, 0, 1, 2, 0, 1, 1] Output: [0, 0, 1, 1, 1, 2, 2]


function isCenturyYear(year) {
    if (year % 100 == 0) {
        return true;  
    } else {
        return false;  
    }
}


let year = 2024;
if (isCenturyYear(year)) {
    console.log(year + " is a century year.");
} else {
    console.log(year + " is not a century year.");
}
